# Дневник разработки — go-kpi-tenders

## 2026-04-25 — День 9: Фикс триггера Python + переход на Celery/Redis

### Часть 1: Фикс триггера Python при создании задачи

#### Контекст

До этого дня цепочка обработки документа выглядела так:

```
Frontend → POST /api/v1/tasks → INSERT в document_tasks → ??? → Python
```

«???» была пропастью. `DocumentTaskService.Create` честно сохранял запись в БД, но никогда не вызывал Python. В итоге `convert` и `anonymize` никогда не стартовали автоматически.

Python-триггер был реализован только в `WorkerService` — для **chaining**: когда `convert` завершается, Go создаёт задачу `anonymize` и зовёт Python. Но **первый** вызов цепочки — `convert` — Python так и не получал.

#### Суть проблемы

`DocumentTaskService` — сервис, который обрабатывает `POST /api/v1/tasks`. Он умел только INSERT. У него не было Python-клиента вообще.

#### Что сделано

`DocumentTaskService` расширен полем `pythonClient workerPythonClient`. После успешного INSERT метод `Create`:

1. Делает `GetDocument`, чтобы достать `storage_path`.
2. Вызывает `pythonClient.Process(...)` — best-effort: ошибки логируются, наружу не уходят.

Два новых теста:
- `TestDocumentTaskService_Create_TriggersPython_WithCorrectFields`
- `TestDocumentTaskService_Create_PythonError_ReturnsTaskWithoutError`

---

### Часть 2: Замена HTTP-клиента на Celery-издатель поверх Redis

#### Контекст

Python-сервис убрал HTTP-эндпоинт `POST /process`. Теперь задачи должны публиковаться напрямую в Redis как Celery-сообщения (`LPUSH`). Это повышает надёжность: если Python-воркеры недоступны, задачи всё равно попадают в очередь и будут обработаны при подъёме воркеров.

#### Что сделано

**1. `internal/pythonworker/client.go`** — полностью переписан:
- Тип `Client` (HTTP) → `Publisher` (Redis/Celery)
- `New(baseURL string) *Client` → `New(redisURL string) (*Publisher, error)` через `redis.ParseURL`
- `Process` формирует Celery protocol v2 сообщение:
  - body = JSON `[[task_id, document_id, storage_path], {}, {callbacks:null,...}]`, base64-encoded
  - headers: `lang`, `task`, `id`, `root_id`, `origin: "go-kpi-tenders"`, ...
  - properties: `correlation_id`, `delivery_mode: 2`, `body_encoding: "base64"`, ...
  - `LPUSH <queue> <json>` в Redis
- `resolveModule` маппит модуль → очередь + имя задачи:

| ModuleName | Queue | Task |
|---|---|---|
| `convert` | `io` | `app.workers.convert.convert_task` |
| `parse_invoice` | `io` | `app.workers.parse_invoice.parse_invoice_task` |
| `anonymize` | `llm` | `app.workers.anonymize.anonymize_task` |
| `extract` | `llm` | `app.workers.extract.extract_task` |

**2. `internal/server/server.go`**:
- `*pythonworker.Client` → `*pythonworker.Publisher`
- Инициализируется через `cfg.RedisURL` (обязателен, `os.Exit(1)` при ошибке парсинга)
- `workerService` создаётся безусловно (Redis всегда есть)

**3. `internal/config/config.go`**:
- Удалено поле `PythonServiceURL` и его URL-валидация
- Удалён импорт `net/url`

**4. Тест-хелперы** `newTestServer()` и `newTestServerWithJWT()` — добавлен `RedisURL: "redis://localhost:6379/0"` (только парсинг, подключения нет).

**5. Зависимость**: добавлен `github.com/redis/go-redis/v9`.

---

### Итог

Полная цепочка теперь работает через Redis/Celery:

```
POST /api/v1/tasks
  → INSERT document_tasks
  → LPUSH io {celery v2 message: convert}
  → Python воркер забирает задачу из очереди
  → PATCH /internal/worker/tasks/:id/status (completed)
  → INSERT document_tasks (anonymize)
  → LPUSH llm {celery v2 message: anonymize}
  → Python воркер забирает задачу
  → PATCH /internal/worker/tasks/:id/status (completed)
```

`make test-unit` — всё зелёное.

### Контекст

До этого дня цепочка обработки документа выглядела так:

```
Frontend → POST /api/v1/tasks → INSERT в document_tasks → ??? → Python
```

«???» была пропастью. `DocumentTaskService.Create` честно сохранял запись в БД, но никогда не вызывал Python `POST /process`. В итоге `convert` и `anonymize` никогда не стартовали автоматически — нужно было либо вызывать Python вручную, либо надеяться на что-то, чего не существовало.

Python-триггер был реализован только в `WorkerService` — для **chaining**: когда `convert` завершается, Go создаёт задачу `anonymize` и зовёт Python. Но **первый** вызов цепочки — `convert` — Python так и не получал.

---

### Суть проблемы

`DocumentTaskService` — сервис, который обрабатывает `POST /api/v1/tasks`. Он умел только INSERT. У него не было Python-клиента вообще.

`WorkerService` — сервис, который обрабатывает callback от Python (`PATCH /internal/worker/tasks/:id/status`). Он умел и триггерить Python (для chaining), и получать обратно результаты. Но он вступает в игру **после** того, как Python уже что-то обработал.

Цепочка никогда не стартовала, потому что первый пинок Python'у так и не происходил.

---

### Что сделано

**1. `DocumentTaskService` — добавлен Python-клиент**

Структура расширена полем `pythonClient workerPythonClient` — тот же consumer-side interface, что использует `WorkerService`. Дублировать интерфейс не нужно: он объявлен один раз в `service_worker.go` и переиспользуется внутри пакета.

Конструктор:
```go
func NewDocumentTaskService(
    repo         repository.Querier,
    pythonClient workerPythonClient, // nil — Python отключён
    log          *slog.Logger,
) *DocumentTaskService
```

**2. Метод `Create` — логика триггера после INSERT**

После успешного `CreateDocumentTask` метод:

1. Делает `GetDocument`, чтобы достать `storage_path` — Python нужен путь к файлу в S3.
2. Вызывает `pythonClient.Process(...)` — тот самый `POST /process`.

Оба шага **best-effort**:
- Если `GetDocument` упал — задача уже в БД, логируем ошибку и возвращаем задачу без ошибки (пользователь может ретриггернуть).
- Если `Process` упал — то же самое: задача в БД, Python-триггер залогирован, ошибка наружу не уходит.

```
POST /api/v1/tasks
  → CreateDocumentTask (INSERT)
  → GetDocument (достать storage_path)
  → pythonClient.Process (POST /process к Python)
  → вернуть задачу клиенту
```

**3. `NewServer()` — единый экземпляр Python-клиента**

Раньше `WorkerService` создавал свой `pythonworker.New(...)` внутри `if`. Теперь клиент создаётся один раз и передаётся в оба сервиса:

```go
var pythonClient *pythonworker.Client
if cfg.PythonServiceURL != "" {
    pythonClient = pythonworker.New(cfg.PythonServiceURL)
} else {
    log.Warn("worker: PYTHON_SERVICE_URL not set — worker callback endpoint disabled")
}

srv.documentTaskService = service.NewDocumentTaskService(db, pythonClient, log)
// workerService оставляем nil если pythonClient == nil
if pythonClient != nil {
    srv.workerService = service.NewWorkerService(db, pythonClient, log)
}
```

Если `PYTHON_SERVICE_URL` не задан — оба сервиса получают `nil`. `DocumentTaskService` guard'ит через `if s.pythonClient != nil`, `WorkerService` вообще не создаётся.

---

### Тесты

Два новых кейса в `service_document_task_test.go`:

| Тест | Что проверяет |
|------|---------------|
| `TestDocumentTaskService_Create_TriggersPython_WithCorrectFields` | `Process` вызван с правильными `task_id`, `document_id`, `module_name`, `storage_path` |
| `TestDocumentTaskService_Create_PythonError_ReturnsTaskWithoutError` | Ошибка Python не пробрасывается — возвращается задача без ошибки |

Существующие 4 теста обновлены под новую сигнатуру конструктора (передают `nil` как pythonClient).

`make test-unit` — всё зелёное.

---

### Итог

Теперь полная цепочка работает:

```
POST /api/v1/tasks
  → INSERT document_tasks
  → Python POST /process (convert)
  → Python PATCH /internal/worker/tasks/:id/status (completed)
  → INSERT document_tasks (anonymize)
  → Python POST /process (anonymize)
  → Python PATCH /internal/worker/tasks/:id/status (completed)
```

Первый и все последующие вызовы Python теперь происходят автоматически.
