# 2026-04-26 — День 9: Watchdog + Артефакты + PR #14 review + Сквош миграций

## Контекст

Большой день: реализован сторожевой таймер зависших задач, хранение AI-артефактов, закрыты все замечания Copilot code review к PR #14, а в конце — все 5 накопившихся миграций сквошены в одну.

---

## Часть 1 — Stale Task Watchdog

Реализован сторожевой таймер для зависших задач (`document_tasks` со статусом `processing`).

### Миграция 000002

**`sql/migrations/000002_stale_task_retry_count.up.sql`**

```sql
ALTER TABLE document_tasks
    ADD COLUMN IF NOT EXISTS retry_count INT NOT NULL DEFAULT 0;

CREATE INDEX IF NOT EXISTS idx_document_tasks_stale
    ON document_tasks (updated_at ASC)
    WHERE status = 'processing';
```

- `retry_count` — счётчик повторных попыток (по умолчанию 0).
- Partial index ускоряет выборку только «живых» processing-задач без full scan.

---

### SQL-запросы (`sql/query/document_task.sql`)

Добавлены три новых запроса:

```sql
-- name: ListStaleTasks :many
SELECT dt.id, dt.document_id, dt.module_name, dt.retry_count, d.storage_path
FROM document_tasks dt JOIN documents d ON d.id = dt.document_id
WHERE dt.status = 'processing' AND dt.updated_at < $1
ORDER BY dt.updated_at ASC;

-- name: MarkStaleTaskPending :execrows
UPDATE document_tasks SET status = 'pending', retry_count = retry_count + 1, updated_at = now()
WHERE id = $1 AND status = 'processing';

-- name: MarkStaleTaskFailed :execrows
UPDATE document_tasks SET status = 'failed',
    error_message = 'stale task: exceeded max retry attempts', updated_at = now()
WHERE id = $1 AND status = 'processing';
```

`MarkStaleTaskPending` и `MarkStaleTaskFailed` используют `WHERE status = 'processing'` как CAS: если другой worker уже забрал задачу, `rowsAffected = 0` и watchdog молча пропускает.

Также `GetDocumentTask` и `ListTasksByDocument` переведены на `dt.*` вместо явного перечисления колонок — это предотвращает генерацию sqlc отдельных `*Row`-типов при JOIN, что ломало бы service-код.

---

### `internal/watchdog`

### Дизайн

```go
type Watchdog struct {
    q   repository.Querier
    pub pythonPublisher   // consumer-side interface: только Process()
    cfg *config.Config
    log *slog.Logger
}
```

- `Start(ctx)` — блокирующий цикл на `time.Ticker` с интервалом `cfg.WatchdogInterval`.
- `runOnce(ctx)` — основная логика: выборка задач старше `cfg.WatchdogThreshold`, обработка каждой.
- `processTask()` — если `RetryCount >= MaxRetries` → `MarkStaleTaskFailed`; иначе → `MarkStaleTaskPending` + `Process()`.
- Ошибки публикации в Redis логируются и не пробрасываются (best-effort).

### Конфиг

```env
WATCHDOG_INTERVAL    = 2m    # частота проверок
WATCHDOG_THRESHOLD   = 10m   # задача считается зависшей
WATCHDOG_MAX_RETRIES = 5     # после этого — permanent fail
```

---

### Подключение в `cmd/api/main.go`

```go
watchdogCtx, watchdogCancel := context.WithCancel(context.Background())
defer watchdogCancel()
go watchdog.Start(watchdogCtx, srv.DB(), srv.PythonPublisher(), cfg, log)
```

Добавлены геттеры `Server.DB()` и `Server.PythonPublisher()` для передачи зависимостей без нарушения инкапсуляции.

---

### Тесты (`internal/watchdog/watchdog_test.go`)

5 unit-кейсов без Docker:

| Тест | Сценарий |
|------|----------|
| `TestRunOnce_RequeuesStaleTask` | normal re-queue: MarkPending + Process |
| `TestRunOnce_FailsTaskWhenMaxRetriesExceeded` | retry_count >= max → MarkFailed, Process не вызывается |
| `TestRunOnce_SkipsWhenNoStaleTasks` | пустой список → ни один метод не вызывается |
| `TestRunOnce_SkipsAlreadyClaimedTask` | MarkPending вернул 0 rows (CAS miss) → Process не вызывается |
| `TestRunOnce_BestEffortOnPublishError` | Process вернул ошибку → задача помечена pending, ошибка залогирована, возврата нет |

---

### Технические нюансы

- **Двойной `make sqlc`**: первый запуск добавил `RetryCount` в `DocumentTask` и создал новые методы; второй запуск (с `dt.*`) убрал лишние `*Row`-типы. После каждого `make sqlc` нужен `make mock` — иначе `mock_querier.go` расходится с интерфейсом.
- `make mock` нужно запускать каждый раз после изменения `repository.Querier`.
- `make migrate_up` нужен перед запуском сервера (применяет 000002 к живой БД).

---

## Часть 2 — Артефакты документов

### Watchdog: покрытие статуса pending

Сторожевой таймер перезапускал только зависшие задачи со статусом `processing`. Добавили `pending` (задача не была подхвачена воркером).

- **Миграция 000003** — расширен partial index `idx_document_tasks_stale`: `WHERE status IN ('pending', 'processing')`.
- `ListStaleTasks`, `MarkStaleTaskPending`, `MarkStaleTaskFailed` обновлены аналогично.
- Тест `TestRunOnce_RequeuesStaleTaskInPendingStatus` добавлен → итого 6 кейсов в `watchdog_test.go`.

### Артефакты документов

Воркер порождает производные документы: convert → markdown, anonymize → анонимизированный + карта сущностей. Реализована полная цепочка хранения артефактов.

#### Миграция 000004

- `documents.artifact_kind VARCHAR(50) NULL` — тип артефакта (`convert_md`, `anonymize_doc`, `anonymize_entities`).
- FK `documents_parent_id_fkey`: `ON DELETE SET NULL` → `ON DELETE CASCADE`.
- Partial index `idx_documents_root ON documents(organization_id, created_at DESC) WHERE parent_id IS NULL`.

### SQL-запросы

| Было | Стало |
|------|-------|
| `ListDocumentsByOrganization` | `ListRootDocumentsByOrganization` (`AND parent_id IS NULL`) |
| `ListDocumentsBySite` / `ListDocumentsBySiteParams` | `ListRootDocumentsBySite` / `ListRootDocumentsBySiteParams` |
| — | `ListDocumentsByParent` — артефакты конкретного родителя |
| `CreateDocument` (8 полей) | `CreateDocument` (9 полей: добавлен `artifact_kind`) |

### Сервисный слой

`DocumentService` — добавлен `ListByParent(ctx, parentID uuid.UUID)`.

`WorkerService` — при `status=completed`:
- **convert** → `registerConvertArtifacts`: создаёт `artifact_kind="convert_md"`, обновляет `result_payload` с UUID.
- **anonymize** → `registerAnonymizeArtifacts`: создаёт `anonymize_doc` + `anonymize_entities`, обновляет `result_payload`.

Оба метода best-effort (ошибки логируются).

### HTTP API

`GET /api/v1/documents?parent_id=<uuid>` — возвращает артефакты документа (с проверкой tenant).

### Тесты

**Unit:**
- `service_worker_test.go` — тест 8 (`ConvertCompleted_RegistersArtifact`), тест 9 (`AnonymizeCompleted_RegistersArtifacts`); тесты 2/5/7 обновлены под новые mock-ожидания.
- `handler_document_test.go` — `TestListDocuments_ByOrg_Success` обновлён: `ListRootDocumentsByOrganization`.

**Интеграционные (`repository_test.go`):**
- `TestRepository_ListRootDocumentsBySite` (переименован).
- `TestRepository_ListRootDocumentsByOrganization_ExcludesArtifacts`.
- `TestRepository_ListDocumentsByParent`.
- `TestRepository_DeleteDocument_CascadesToArtifacts`.

### Итог

```text
make test-unit        → 68 тестов, все PASS
make test-integration → 22 теста, все PASS
```

---

## Часть 3 — PR #14 code review: 5 замечаний

Copilot code review на PR #14 выдал 5 замечаний. Часть касалась корректности SQL (race condition), часть — защитного программирования (`fileNameFromPath`, валидация конфига), часть — полноты схемы (`ON CONFLICT` для артефактов). После закрытия всех замечаний приняли решение сквошить все 5 накопившихся миграций в одну.

---

### 1. TOCTOU-фикс в watchdog SQL

`MarkStaleTaskPending` и `MarkStaleTaskFailed` обновляли задачу только по `id`. Два конкурирующих экземпляра watchdog могли захватить одну задачу одновременно: оба видят её в `ListStaleTasks`, оба делают UPDATE, оба публикуют в Redis — задача выполняется дважды.

Исправление: `AND updated_at < $2` в оба UPDATE. Параметр — `cutoff` (тот же timestamp, что и в `ListStaleTasks`). UPDATE атомарно проверяет, что задача не изменилась после листинга:

```sql
UPDATE document_tasks
SET    status = 'pending', retry_count = retry_count + 1, updated_at = now()
WHERE  id = $1
  AND  updated_at < $2
RETURNING *;
```

Второй конкурент получит 0 строк → `pgx.ErrNoRows` → CAS-пропуск.

`processTask` расширена сигнатурой `cutoff time.Time`; `runOnce` вычисляет и передаёт `cutoff` в оба вызова.

**Тесты**: 5 тестов обновлены под `MarkStale*Params` через `mock.MatchedBy(func(p ...) bool { return p.ID == taskID })` — нечувствительно к конкретному значению `cutoff`.

### 2. `CreateArtifactDocument` — `DO UPDATE SET` вместо `DO NOTHING`

При повторном callback воркера (retry, idempotent replay) файл в MinIO мог обновиться, а запись в БД оставалась старой — `DO NOTHING` молча игнорировал новые данные.

Исправлено на:

```sql
ON CONFLICT (parent_id, artifact_kind) WHERE parent_id IS NOT NULL
DO UPDATE SET
    file_name       = EXCLUDED.file_name,
    storage_path    = EXCLUDED.storage_path,
    mime_type       = EXCLUDED.mime_type,
    file_size_bytes = EXCLUDED.file_size_bytes,
    updated_at      = now();
```

### 3. Валидация watchdog-конфига

`validate()` не проверяла поля Watchdog. `WATCHDOG_INTERVAL=0` → спиннинг; `WATCHDOG_THRESHOLD=-1m` → все задачи считаются зависшими немедленно.

Добавлены три проверки:

```go
if c.WatchdogInterval <= 0 { return fmt.Errorf("WATCHDOG_INTERVAL must be positive (got %s)", ...) }
if c.WatchdogThreshold <= 0 { return fmt.Errorf("WATCHDOG_THRESHOLD must be positive (got %s)", ...) }
if c.WatchdogMaxRetries < 0 { return fmt.Errorf("WATCHDOG_MAX_RETRIES must be >= 0 (got %d)", ...) }
```

### 4. `fileNameFromPath` → `path.Base`

`strings.Split("bucket/docs/", "/")` → последний элемент `""`. Для MinIO-путей с trailing slash функция возвращала пустую строку.

```go
// было
parts := strings.Split(storagePath, "/")
return parts[len(parts)-1]

// стало
func fileNameFromPath(storagePath string) string {
    if storagePath == "" {
        return ""
    }
    return path.Base(storagePath)
}
```

`path.Base` trim'ит trailing slashes перед извлечением имени.

### 5. Бэкфилл `input_storage_path` в миграции

Колонка добавлялась с `DEFAULT ''`, но уже существующие `convert`-задачи имели реальные пути к файлам. Watchdog при перезапуске таких задач отправлял воркеру пустой путь.

Добавлен бэкфилл: UPDATE через JOIN для `convert`, затем fallback для остальных задач с непустым `storage_path`.

### 6. `parent_id` убран из публичного `POST /api/v1/documents`

CHECK constraint `documents_parent_artifact_kind_chk` требует, чтобы при `parent_id IS NOT NULL` поле `artifact_kind` тоже было задано. Публичный хендлер `CreateDocument` принимал `parent_id`, но `artifact_kind` в запросе не было — любой `parent_id` в теле запроса приводил бы к нарушению constraint на INSERT.

Решение (вариант a): `parent_id` убран из `createDocumentRequest`. Артефакты создаёт только воркер через `CreateArtifactDocument` — с явным `artifact_kind`. Публичный эндпоинт создаёт исключительно оригинальные документы (`parent_id IS NULL`, `artifact_kind IS NULL`).

### 7. Retry-loop при публикации в watchdog

`MarkStaleTaskPending` обновляет `updated_at = now()` до попытки отправки в Redis. Если Redis недоступен — задача остаётся в `pending` с "свежим" timestamp'ом и не будет считаться зависшей до следующего цикла (через `WatchdogThreshold`).

Добавлен retry-loop: 3 попытки с 500ms паузой между ними. После исчерпания попыток — best-effort логирование, задача гарантированно будет перезапущена на следующем тике watchdog'а.

```go
const maxPublishAttempts = 3
const publishAttemptTimeout = 5 * time.Second
const publishRetryDelay = 500 * time.Millisecond
```

Тест `TestRunOnce_BestEffortOnPublishError` обновлён: `.Times(3)` — ожидает ровно 3 вызова `Process`.

### 8. `sync.WaitGroup` для корректного shutdown watchdog-горутины

`watchdogCancel()` отменяет контекст горутины, но не ждёт её завершения. Если `srv.Close()` (закрывает пул Redis-соединений) вызывается раньше, чем watchdog выходит из текущего `processTask`, возникает race: in-flight `LPUSH` использует уже закрытый пул.

```go
var watchdogDone sync.WaitGroup
watchdogDone.Add(1)
go func() {
    defer watchdogDone.Done()
    watchdog.Start(watchdogCtx, srv.DB(), srv.PythonPublisher(), cfg, log)
}()

// shutdown:
watchdogCancel()
watchdogDone.Wait() // горутина завершена → Redis закрывать безопасно
```

Подпись `watchdog.Start` не менялась — `WaitGroup` инкапсулирован в `cmd/api/main.go`. В импорты добавлен `"sync"`.

### 9. Константа `moduleConvert` вместо хардкода в `service_document_task.go`

`DocumentTaskService.Create` проверял `params.ModuleName != "convert"` — строковый литерал. Константа `moduleConvert = "convert"` уже была объявлена в `service_worker.go` (тот же пакет `service`).

```go
// было
if params.ModuleName != "convert" {
    return ..., errs.New(..., "only 'convert' is allowed", nil)
}

// стало
if params.ModuleName != moduleConvert {
    return ..., errs.New(..., fmt.Sprintf("only %q is allowed", moduleConvert), nil)
}
```

Сообщение об ошибке также использует константу через `%q` — при переименовании модуля текст ошибки обновится автоматически.

### 10. Покрытие хендлера для ветки `?parent_id=`

Copilot отметил, что `ListDocuments` содержит новую ветку (`parent_id` query-параметр) без единого теста. Добавлено три кейса в `handler_document_test.go`:

| Тест | Сценарий | HTTP |
|------|----------|------|
| `TestListDocuments_ByParentID_Success` | valid UUID → `GetDocument` (ownership check) + `ListDocumentsByParent` → 200 + массив артефактов | 200 |
| `TestListDocuments_InvalidParentID_Returns400` | `parent_id=not-a-uuid` → uuid.Parse падает → validation_failed | 400 |
| `TestListDocuments_ParentNotFound_Returns404` | `GetDocument` возвращает `pgx.ErrNoRows` (parent из чужого org) → not_found | 404 |

---

## Часть 4 — Сквош миграций 000001–000005

После закрытия всех замечаний в БД накопилось 5 миграций. Проект ещё не деплоился в production — история без нужды не нужна.

```bash
migrate ... down 5               # откат всех 5 (БД → version 0)
rm 000002_*.sql 000003_*.sql \
   000004_*.sql 000005_*.sql     # удалено 8 файлов
```

`000001_init_schema.up.sql` переписан: единый файл сразу содержит всё — `artifact_kind`, `parent_id ON DELETE CASCADE`, `retry_count`, `input_storage_path`, 11 индексов (3 partial: `idx_document_tasks_stale`, `idx_documents_root`, `idx_documents_artifact_kind`), `COMMENT ON TABLE/COLUMN` для каждой таблицы и значимых полей.

`make migrate_up` → `1/u init_schema (2.2s)`.

CLAUDE.md: таблица миграций → одна строка; следующая миграция — `000002` (`catalog_positions`, pgvector RAG).

---

## Итог дня

```text
make test-unit        → PASS
make test-integration → 29/29 PASS (26s)
```

База: 5 таблиц, 11 индексов, 3 функции + 5 триггеров — из единственной миграции `000001`.

---

## Часть 5 — PR #14 review items 5–10 (сессии 3–4)

Продолжение Части 3: реализованы оставшиеся 5 тяжёлых замечаний.

### Item 5: MarkStaleTaskPending сбрасывает поля

`MarkStaleTaskPending`: добавлены `celery_task_id = NULL, error_message = NULL` в SET clause.

### Item 6: defer leak — runWithArtifactTimeout

Оба `defer artifactCancel()` в `service_worker.go` были scoped к горутине, а не к вызову.
Вынесен хелпер `runWithArtifactTimeout` с корректным `defer cancel()`.

### Item 7: ListStaleTasks LIMIT + WatchdogBatchSize

`ListStaleTasks` теперь принимает `LIMIT $2`. Новый конфиг `WATCHDOG_BATCH_SIZE` (default 100).

### Item 8: MarkStaleTaskFailed — параметризован error_message

`error_message = 'stale task...'` → `$3`. Динамическое сообщение с retry counts.

### Item 9: UpdateTaskResultPayload

Новый SQL-запрос и метод репозитория. В `service_worker.go` оба
`registerConvertArtifacts` / `registerAnonymizeArtifacts` используют его вместо
`UpdateWorkerTaskStatus` для обновления только `result_payload`. Тесты обновлены.

### Item 10: Комментарий на CreateDocumentTask

`-- Callers MUST map pgx.ErrNoRows to 404/403` добавлен в SQL-файл.

### Финальный статус

`make test-unit` — 5 пакетов PASS, 0 FAIL.

---

## Часть 6 — PR #14 re-review: 8 замечаний CodeRabbit (сессия 5)

Второй round CodeRabbit на PR #14. 8 замечаний, 7 реализовано, 1 отклонено.

### Item 1: SQL comment 404 вместо 404/403

`CreateDocumentTask` — комментарий `-- Callers MUST map pgx.ErrNoRows to 404/403` уточнён:
`-- map to 404: intentionally reported as 404 to avoid leaking tenant existence`.
Тенант-утечка через разные HTTP-коды — стандартный OWASP-паттерн.

### Item 2: sqlc.arg — именованные параметры для watchdog-запросов

Все позиционные `$N` в watchdog-запросах заменены на `sqlc.arg(name)`:

```sql
-- ListStaleTasks
WHERE dt.status IN ('pending','processing')
  AND dt.updated_at < sqlc.arg(cutoff)
ORDER BY dt.updated_at ASC
LIMIT sqlc.arg(batch_size)

-- MarkStaleTaskPending / MarkStaleTaskFailed
AND updated_at < sqlc.arg(cutoff)

-- MarkStaleTaskFailed
error_message = sqlc.arg(error_message)
```

Sqlc генерирует struct-поля с читаемыми именами вместо `UpdatedAt`/`Limit`:
`ListStaleTasksParams{Cutoff, BatchSize}`, `MarkStaleTaskPendingParams{ID, Cutoff}`,
`MarkStaleTaskFailedParams{ID, ErrorMessage, Cutoff}`.

Обновлены все три caller-сайта в `watchdog.go` и 6 матчеров в `watchdog_test.go`
(`p.Limit` → `p.BatchSize`).

**Нюанс:** смешивать позиционные `$N` и `sqlc.arg()` в одном запросе нельзя —
sqlc не выводит тип для `$2` в этом случае. `error_message = $3` тоже переведён
на `sqlc.arg(error_message)`.

### Item 3: Девлог — формулировка «11 partial-индексов»

Исправлено на «11 индексов (3 partial: idx_document_tasks_stale, idx_documents_root,
idx_documents_artifact_kind)».

### Item 4: UpdateTaskResultPayload — комментарий о tenant-guard

Добавлен комментарий в SQL:
`-- Internal: no org-check; callers must be authenticated via SERVICE_TOKEN.`

### Item 5: pc.AssertExpectations в тесте RegistersArtifact

Тест `TestWorkerService_HandleStatusUpdate_ConvertCompleted_RegistersArtifact`
не вызывал `pc.AssertExpectations(t)`. Добавлено рядом с `ms.AssertExpectations(t)`.

### Item 6: Watchdog startup log — batch_size

Лог запуска watchdog не включал `batch_size`:

```go
// было
slog.Duration("interval", cfg.WatchdogInterval),
slog.Duration("threshold", cfg.WatchdogThreshold),
slog.Int("max_retries", cfg.WatchdogMaxRetries),

// стало
slog.Int("batch_size", cfg.WatchdogBatchSize),  // добавлено
```

### Item 7: input_storage_path — CHECK вместо DEFAULT ''

`DEFAULT ''` убран, добавлен CHECK:

```sql
-- было
input_storage_path TEXT NOT NULL DEFAULT ''

-- стало
input_storage_path TEXT NOT NULL
    CHECK (btrim(input_storage_path) <> '')
```

Пустая строка — признак незаполненного пути; БД теперь отклоняет такие INSERT.
Миграция откатана и накатана заново.

### Item 8: CHECK на artifact_kind allowed values — ОТКЛОНЕНО

CodeRabbit предложил `CHECK (artifact_kind IN ('converted', 'anonymized', ...))`,
но это заблокировало бы добавление новых AI-модулей (parse_invoice, extract)
без миграции. Решение: валидация на уровне приложения, схема остаётся расширяемой.

### Финальный статус

```text
make sqlc             → OK (после фикса sqlc.arg в error_message)
make mock             → OK
migrate down 1 + up   → OK (1/u init_schema)
make test-unit        → 5 пакетов PASS, 0 FAIL
```

---

## Часть 7 — PR #14 re-review: дополнительные 2 замечания (сессия 6)

### Item 1: README — WATCHDOG_BATCH_SIZE в примере .env

Copilot заметил, что `WATCHDOG_BATCH_SIZE` есть в коде (валидация, дефолт 100), но в README его нет.
Оператор не узнал бы об этой переменной без чтения кода.

```diff
 WATCHDOG_MAX_RETRIES=5
+WATCHDOG_BATCH_SIZE=100
```

### Item 2: registerConvertArtifacts — защита result_payload при ошибке triggerAnonymize

**Проблема:** если `triggerAnonymize` падал, код всё равно вызывал `registerConvertArtifacts`,
который перезаписывал `result_payload` в БД: `md_storage_path` заменялся на
`md_document_id`. Retry анонимизации становился невозможен.

**Исправление:** `registerConvertArtifacts` перенесён в `else`-ветку:

```go
// было
if err := s.triggerAnonymize(...); err != nil {
    s.log.Error(...)
}
if err := runWithArtifactTimeout(..., s.registerConvertArtifacts); err != nil { ... }

// стало
if err := s.triggerAnonymize(...); err != nil {
    // Intentionally skip artifact registration here to preserve the
    // original result_payload (md_storage_path) for a future retry.
    s.log.Error(...)
} else {
    if err := runWithArtifactTimeout(..., s.registerConvertArtifacts); err != nil { ... }
}
```

Теперь: если анонимизация не запущена, `md_storage_path` остаётся в `result_payload` —
доступен для будущего retry.

**Тест:** `PythonClientError_NoErrorPropagated` очищен от ожиданий
`GetDocumentByID`, `CreateArtifactDocument`, `UpdateTaskResultPayload` —
`AssertExpectations` теперь реально проверяет, что эти методы **не вызываются**.

### Финальный статус

```text
make test-unit        → 5 пакетов PASS, 0 FAIL
```

---

## Часть 8 — PR #14 re-review: 5 замечаний (сессия 7)

### Item 1: SQL comment ListStaleTasks — `$1` → `sqlc.arg(cutoff)`

Copilot: комментарий всё ещё ссылался на позиционный `$1`, хотя запрос давно использует `sqlc.arg`.

```sql
-- было
-- Watchdog: returns stuck tasks ... older than $1 (cutoff timestamp). ...

-- стало
-- Watchdog: returns stuck tasks ... older than sqlc.arg(cutoff).
-- Results are limited by sqlc.arg(batch_size). ...
```

### Item 2: README.md — ```text в 3 блоках маршрутов (MD040)

CodeRabbit/markdownlint MD040: анонимные fenced code blocks должны иметь language tag.
Блоки «Задачи обработки», «Объекты строительства», «Internal (Python-воркер)» получили ` ```text `.

### Item 3: Миграция — `btrim(artifact_kind) <> ''` в CHECK

CodeRabbit: `documents_parent_artifact_kind_chk` пропускал пустые/пробельные `artifact_kind`.

```sql
-- было
CONSTRAINT documents_parent_artifact_kind_chk CHECK (
    (parent_id IS NULL AND artifact_kind IS NULL) OR
    (parent_id IS NOT NULL AND artifact_kind IS NOT NULL)
)

-- стало
CONSTRAINT documents_parent_artifact_kind_chk CHECK (
    (parent_id IS NULL AND artifact_kind IS NULL) OR
    (parent_id IS NOT NULL AND artifact_kind IS NOT NULL AND btrim(artifact_kind) <> '')
)
```

Миграция откатана и накатана заново.

### Item 4: Русский комментарий в service_worker.go → английский

```diff
-// Регистрация артефактов anonymize
+// Register anonymize artifacts.
```

### Item 5: watchdog.go — ctx.Err() check в retry loop

CodeRabbit: context cancellation при graceful shutdown попадала в финальный `Error`-лог.
Исправление: проверка `ctx.Err()` в начале каждой итерации retry loop:

```go
for attempt := 1; attempt <= maxPublishAttempts; attempt++ {
    if ctx.Err() != nil {
        taskLog.Info("watchdog: context canceled, aborting task re-queue")
        return
    }
    // ...
}
```

Теперь остановка при шатдауне логируется как `Info`, а не `Error`.

### Пропущено

- **service_worker.go** silent failure → persist recovery: архитектурная фича (reconciler, новый статус), не fix
- **watchdog_test.go** configurable retry delay: рефакторинг ради скорости теста, не баг

### Финальный статус

```text
migrate down 1 + up   → OK
make test-unit        → 5 пакетов PASS, 0 FAIL
```

---

## Часть 9 — PR #14 re-review: parent_id в UploadDocument (сессия 7)

### Проблема

Copilot: в `createDocumentRequest` (JSON endpoint) `parent_id` был удалён ранее,
но `UploadDocument` (multipart) всё ещё читал `parent_id` из form-поля и писал
`params.ParentID` без `artifact_kind`. С новым CHECK-constraint
`documents_parent_artifact_kind_chk` (`parent_id IS NOT NULL → artifact_kind IS NOT NULL AND btrim <> ''`)
любой upload с `parent_id` падал бы на INSERT с 500.

### Исправление

Удалён блок `parent_id` из `UploadDocument`:

```diff
-	if parentIDStr := c.PostForm("parent_id"); parentIDStr != "" {
-		id, err := uuid.Parse(parentIDStr)
-		...
-		params.ParentID = pgtype.UUID{Bytes: id, Valid: true}
-	}
```

Обновлён doc comment — строка `parent_id (optional)` убрана.

Артефакты (документы с `parent_id`) создаёт только воркер через
`CreateArtifactDocument` с явным `artifact_kind`. Публичный upload-эндпоинт
создаёт исключительно оригиналы.

### Финальный статус

```text
make test-unit        → 5 пакетов PASS, 0 FAIL
```
