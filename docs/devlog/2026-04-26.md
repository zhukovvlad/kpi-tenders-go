# 2026-04-26 — Stale Task Watchdog + PR #14 Review Fixes

## Что сделано (сессия 2 — PR #14 review fixes)

Реализованы все замечания code review к PR #14.

---

## Миграция 000005

**`sql/migrations/000005_input_storage_path_artifact_unique`**

```sql
ALTER TABLE document_tasks ADD COLUMN input_storage_path TEXT NOT NULL DEFAULT '';
UPDATE document_tasks dt SET input_storage_path = d.storage_path
    FROM documents d WHERE dt.document_id = d.id AND dt.module_name = 'convert';
CREATE UNIQUE INDEX idx_documents_artifact_kind
    ON documents (parent_id, artifact_kind) WHERE parent_id IS NOT NULL;
```

- `input_storage_path` — путь к входному файлу для каждой задачи. Для `convert` = `documents.storage_path`; для `anonymize` = путь к Markdown-артефакту предыдущего шага.
- UNIQUE index предотвращает дублирование артефактов при повторных попытках воркера.

---

## SQL-запросы

### `document_task.sql`

- `CreateDocumentTask`: INSERT теперь сам читает `storage_path` из `documents` через subquery (нет отдельного round-trip в код).
- `CreateDocumentTaskInternal`: добавлен `$3` — `input_storage_path`; `ON CONFLICT DO NOTHING` — идемпотентно.
- `ListStaleTasks`: JOIN на `documents` убран — путь берётся из `dt.input_storage_path`.

### `document.sql`

- `ListDocumentsByParent`: добавлен `AND organization_id = $2` — tenant isolation.
- `CreateArtifactDocument` (новый): `ON CONFLICT (parent_id, artifact_kind) WHERE parent_id IS NOT NULL DO UPDATE SET file_name = EXCLUDED.file_name RETURNING *` — идемпотентный upsert артефактов.

---

## Сервисный слой

- `service_document.go`: `ListByParent(ctx, parentID, orgID uuid.UUID)` — добавлен `orgID`.
- `handler_document.go`: передаёт `orgID` из JWT-контекста.
- `service_worker.go`:
  - `triggerAnonymize`: `CreateDocumentTaskInternal` получает `InputStoragePath: payload.MDStoragePath`.
  - `registerArtifact`: использует `CreateArtifactDocument` вместо `CreateDocument` — идемпотентно.
  - `registerAnonymizeArtifacts`: ранний возврат если оба пути пустые (предотвращает затирание `result_payload` пустыми UUID).

---

## Graceful shutdown (main.go)

```go
// Было: defer watchdogCancel() — выполнялся после srv.Close()
// Стало: явный вызов сразу при получении сигнала
watchdogCancel()
if err := httpSrv.Shutdown(shutdownCtx); err != nil { ... }
srv.Close()
```

Watchdog останавливается до закрытия пула Redis — устранён race condition.

---

## Мелкие правки

- `internal/config/config.go`: комментарий `WatchdogThreshold` — `'processing'` → `'pending' or 'processing'`.
- `internal/watchdog/watchdog.go`: комментарии обновлены аналогично.
- `internal/server/server.go`: комментарий `PythonPublisher()` — убрана вводящая в заблуждение фраза про nil.
- `README.md`: `REDIS_URL=redis://localhost:6379` → `redis://localhost:6379/0`; добавлены language tags к code fences; добавлена строка миграции 000005.

---

## Тесты

Обновлены под новые сигнатуры:

- `service_worker_test.go`: `CreateDocumentTaskInternal` получает `InputStoragePath`; `CreateDocument` → `CreateArtifactDocument` в тестах 2, 5, 7, 8, 9.
- `watchdog_test.go`: `ListStaleTasksRow.StoragePath` → `InputStoragePath` в трёх тест-кейсах.
- `tests/integration/repository_test.go`: `ListDocumentsByParent` принимает `ListDocumentsByParentParams{ParentID, OrganizationID}`.

Итого: **все unit и integration тесты зелёные** (`make test-unit` + `make test-integration`).

---

## Что сделано (сессия 1 — Stale Task Watchdog)

Реализован сторожевой таймер для зависших задач (`document_tasks` со статусом `processing`).

---

## Миграция 000002

**`sql/migrations/000002_stale_task_retry_count.up.sql`**

```sql
ALTER TABLE document_tasks
    ADD COLUMN IF NOT EXISTS retry_count INT NOT NULL DEFAULT 0;

CREATE INDEX IF NOT EXISTS idx_document_tasks_stale
    ON document_tasks (updated_at ASC)
    WHERE status = 'processing';
```

- `retry_count` — счётчик повторных попыток (по умолчанию 0).
- Partial index ускоряет выборку только «живых» processing-задач без full scan.

---

## SQL-запросы (`sql/query/document_task.sql`)

Добавлены три новых запроса:

```sql
-- name: ListStaleTasks :many
SELECT dt.id, dt.document_id, dt.module_name, dt.retry_count, d.storage_path
FROM document_tasks dt JOIN documents d ON d.id = dt.document_id
WHERE dt.status = 'processing' AND dt.updated_at < $1
ORDER BY dt.updated_at ASC;

-- name: MarkStaleTaskPending :execrows
UPDATE document_tasks SET status = 'pending', retry_count = retry_count + 1, updated_at = now()
WHERE id = $1 AND status = 'processing';

-- name: MarkStaleTaskFailed :execrows
UPDATE document_tasks SET status = 'failed',
    error_message = 'stale task: exceeded max retry attempts', updated_at = now()
WHERE id = $1 AND status = 'processing';
```

`MarkStaleTaskPending` и `MarkStaleTaskFailed` используют `WHERE status = 'processing'` как CAS: если другой worker уже забрал задачу, `rowsAffected = 0` и watchdog молча пропускает.

Также `GetDocumentTask` и `ListTasksByDocument` переведены на `dt.*` вместо явного перечисления колонок — это предотвращает генерацию sqlc отдельных `*Row`-типов при JOIN, что ломало бы service-код.

---

## `internal/watchdog`

### Дизайн

```go
type Watchdog struct {
    q   repository.Querier
    pub pythonPublisher   // consumer-side interface: только Process()
    cfg *config.Config
    log *slog.Logger
}
```

- `Start(ctx)` — блокирующий цикл на `time.Ticker` с интервалом `cfg.WatchdogInterval`.
- `runOnce(ctx)` — основная логика: выборка задач старше `cfg.WatchdogThreshold`, обработка каждой.
- `processTask()` — если `RetryCount >= MaxRetries` → `MarkStaleTaskFailed`; иначе → `MarkStaleTaskPending` + `Process()`.
- Ошибки публикации в Redis логируются и не пробрасываются (best-effort).

### Конфиг

```
WATCHDOG_INTERVAL    = 2m    # частота проверок
WATCHDOG_THRESHOLD   = 10m   # задача считается зависшей
WATCHDOG_MAX_RETRIES = 5     # после этого — permanent fail
```

---

## Подключение в `cmd/api/main.go`

```go
watchdogCtx, watchdogCancel := context.WithCancel(context.Background())
defer watchdogCancel()
go watchdog.Start(watchdogCtx, srv.DB(), srv.PythonPublisher(), cfg, log)
```

Добавлены геттеры `Server.DB()` и `Server.PythonPublisher()` для передачи зависимостей без нарушения инкапсуляции.

---

## Тесты (`internal/watchdog/watchdog_test.go`)

5 unit-кейсов без Docker:

| Тест | Сценарий |
|------|----------|
| `TestRunOnce_RequeuesStaleTask` | normal re-queue: MarkPending + Process |
| `TestRunOnce_FailsTaskWhenMaxRetriesExceeded` | retry_count >= max → MarkFailed, Process не вызывается |
| `TestRunOnce_SkipsWhenNoStaleTasks` | пустой список → ни один метод не вызывается |
| `TestRunOnce_SkipsAlreadyClaimedTask` | MarkPending вернул 0 rows (CAS miss) → Process не вызывается |
| `TestRunOnce_BestEffortOnPublishError` | Process вернул ошибку → задача помечена pending, ошибка залогирована, возврата нет |

---

## Технические нюансы

- **Двойной `make sqlc`**: первый запуск добавил `RetryCount` в `DocumentTask` и создал новые методы; второй запуск (с `dt.*`) убрал лишние `*Row`-типы. После каждого `make sqlc` нужен `make mock` — иначе `mock_querier.go` расходится с интерфейсом.
- `make mock` нужно запускать каждый раз после изменения `repository.Querier`.
- `make migrate_up` нужен перед запуском сервера (применяет 000002 к живой БД).

---

# 2026-04-26 (продолжение) — Артефакты документов

## Watchdog: покрытие статуса pending

Сторожевой таймер перезапускал только зависшие задачи со статусом `processing`. Добавили `pending` (задача не была подхвачена воркером).

- **Миграция 000003** — расширен partial index `idx_document_tasks_stale`: `WHERE status IN ('pending', 'processing')`.
- `ListStaleTasks`, `MarkStaleTaskPending`, `MarkStaleTaskFailed` обновлены аналогично.
- Тест `TestRunOnce_RequeuesStaleTaskInPendingStatus` добавлен → итого 6 кейсов в `watchdog_test.go`.

## Артефакты документов

Воркер порождает производные документы: convert → markdown, anonymize → анонимизированный + карта сущностей. Реализована полная цепочка хранения артефактов.

### Миграция 000004

- `documents.artifact_kind VARCHAR(50) NULL` — тип артефакта (`convert_md`, `anonymize_doc`, `anonymize_entities`).
- FK `documents_parent_id_fkey`: `ON DELETE SET NULL` → `ON DELETE CASCADE`.
- Partial index `idx_documents_root ON documents(organization_id, created_at DESC) WHERE parent_id IS NULL`.

### SQL-запросы

| Было | Стало |
|------|-------|
| `ListDocumentsByOrganization` | `ListRootDocumentsByOrganization` (`AND parent_id IS NULL`) |
| `ListDocumentsBySite` / `ListDocumentsBySiteParams` | `ListRootDocumentsBySite` / `ListRootDocumentsBySiteParams` |
| — | `ListDocumentsByParent` — артефакты конкретного родителя |
| `CreateDocument` (8 полей) | `CreateDocument` (9 полей: добавлен `artifact_kind`) |

### Сервисный слой

`DocumentService` — добавлен `ListByParent(ctx, parentID uuid.UUID)`.

`WorkerService` — при `status=completed`:
- **convert** → `registerConvertArtifacts`: создаёт `artifact_kind="convert_md"`, обновляет `result_payload` с UUID.
- **anonymize** → `registerAnonymizeArtifacts`: создаёт `anonymize_doc` + `anonymize_entities`, обновляет `result_payload`.

Оба метода best-effort (ошибки логируются).

### HTTP API

`GET /api/v1/documents?parent_id=<uuid>` — возвращает артефакты документа (с проверкой tenant).

### Тесты

**Unit:**
- `service_worker_test.go` — тест 8 (`ConvertCompleted_RegistersArtifact`), тест 9 (`AnonymizeCompleted_RegistersArtifacts`); тесты 2/5/7 обновлены под новые mock-ожидания.
- `handler_document_test.go` — `TestListDocuments_ByOrg_Success` обновлён: `ListRootDocumentsByOrganization`.

**Интеграционные (`repository_test.go`):**
- `TestRepository_ListRootDocumentsBySite` (переименован).
- `TestRepository_ListRootDocumentsByOrganization_ExcludesArtifacts`.
- `TestRepository_ListDocumentsByParent`.
- `TestRepository_DeleteDocument_CascadesToArtifacts`.

### Итог

```
make test-unit        → 68 тестов, все PASS
make test-integration → 22 теста, все PASS
```
